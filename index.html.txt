<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forex Expert | Digitaly King</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    
    <style>
        :root {
            --primary-color: #f39c12; /* Gold color */
            --bg-dark: #0f172a;
            --card-bg: #1e293b;
            --text-main: #ffffff;
            --text-dim: #94a3b8;
            --tg-blue: #0088cc;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--bg-dark);
            color: var(--text-main);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            max-width: 450px;
            width: 100%;
            background: var(--card-bg);
            padding: 40px 30px;
            border-radius: 24px;
            text-align: center;
            box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.3);
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .profile-img {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            margin-bottom: 20px;
            border: 4px solid var(--primary-color);
            object-fit: cover;
            transition: transform 0.3s ease;
        }

        .profile-img:hover {
            transform: scale(1.05);
        }

        h1 {
            font-size: 28px;
            margin-bottom: 5px;
            font-weight: 700;
            letter-spacing: -0.5px;
        }

        .sub-count {
            color: var(--primary-color);
            font-weight: 600;
            font-size: 18px;
            margin-bottom: 25px;
            display: block;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 15px;
            margin-bottom: 30px;
        }

        .stat-box {
            background: rgba(255, 255, 255, 0.05);
            padding: 15px;
            border-radius: 12px;
        }

        .stat-box h3 {
            font-size: 18px;
            color: var(--primary-color);
        }

        .stat-box p {
            font-size: 13px;
            color: var(--text-dim);
        }

        .features {
            text-align: left;
            margin-bottom: 30px;
        }

        .feature-item {
            display: flex;
            align-items: flex-start;
            margin-bottom: 15px;
            font-size: 15px;
            line-height: 1.4;
        }

        .feature-item i {
            color: var(--primary-color);
            margin-right: 12px;
            margin-top: 4px;
        }

        .cta-button {
            display: flex;
            align-items: center;
            justify-content: center;
            background: var(--tg-blue);
            color: white;
            text-decoration: none;
            padding: 18px;
            border-radius: 12px;
            font-weight: 700;
            font-size: 18px;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(0, 136, 204, 0.3);
            margin-bottom: 20px;
        }

        .cta-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(0, 136, 204, 0.4);
            filter: brightness(1.1);
        }

        .cta-button i {
            margin-right: 10px;
            font-size: 22px;
        }

        .footer-text {
            font-size: 13px;
            color: var(--text-dim);
            line-height: 1.6;
        }

        .footer-text a {
            color: var(--tg-blue);
            text-decoration: none;
            font-weight: 600;
        }

        /* Animation */
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .container {
            animation: fadeIn 0.8s ease-out;
        }
    </style>
</head>
<body>

    <div class="container">
        <img src="https://digitalyking.com/wp-content/uploads/2025/07/my_image1743060797-2025-27-03-07-33-18.webp" alt="Forex Expert" class="profile-img">

        <h1>Forex Expert</h1>
        <span class="sub-count">18,970+ Subscribers</span>

        <div class="stats-grid">
            <div class="stat-box">
                <h3>10+ Yrs</h3>
                <p>Experience</p>
            </div>
            <div class="stat-box">
                <h3>7,000+</h3>
                <p>Daily Followers</p>
            </div>
        </div>

        <div class="features">
            <div class="feature-item">
                <i class="fas fa-chart-line"></i>
                <p>Earn With Forex Academy! 📈🎓</p>
            </div>
            <div class="feature-item">
                <i class="fas fa-key"></i>
                <p>Discover a secret, profitable method. 💼💸</p>
            </div>
            <div class="feature-item">
                <i class="fas fa-users"></i>
                <p>Trusted by 18K+ Global Users. 🤝💯</p>
            </div>
            <div class="feature-item">
                <i class="fas fa-unlock-alt"></i>
                <p>100% Supporting Community & Instant Access!</p>
            </div>
        </div>

        <a href="https://telegram.me/+Uwc4UokR7KM2NjY1" class="cta-button">
            <i class="fab fa-telegram-plane"></i> Join Our Community
        </a>

        <p class="footer-text">
            If you have Telegram, you can view and join 
            <a href="https://telegram.me/+j4-4rRJb6n00ZWY1">Forex & Crypto Expert</a> 
            right away.
        </p>
    </div>

</body>
</html>